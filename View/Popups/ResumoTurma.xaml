<Window x:Class="TCC_Assiduidade.View.Popups.ResumoTurma"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        mc:Ignorable="d" 
        Title="Relatório da Turma" 
        Height="600" Width="800"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Window.Resources>
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#236B73"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="10">
                            <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="White" CornerRadius="12" Margin="15" Padding="25">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" Direction="-90" ShadowDepth="2" Opacity="0.25"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,15">
                <TextBlock Text="Relatório da Turma" FontSize="20" FontWeight="Bold" Foreground="#11181C"/>
                <Button Content="✕" HorizontalAlignment="Right" Background="Transparent" BorderThickness="0" Foreground="#888" FontSize="18" FontWeight="Bold" Cursor="Hand" FocusVisualStyle="{x:Null}" Click="BtnFechar_Click"/>
            </Grid>

            <Border Grid.Row="1" Background="#ECF8F6" CornerRadius="8" Padding="15" Margin="0,0,0,15" BorderBrush="#B0C8D6" BorderThickness="1">
                <StackPanel Orientation="Horizontal">
                    <TextBlock FontSize="14" Foreground="#1F2933" Margin="0,0,20,0">
                        <Run Text="Turma: " FontWeight="Bold" Foreground="#236B73"/>
                        <Run Text="{Binding TurmaInfo.Nome, FallbackValue='-'}" />
                    </TextBlock>
                    <TextBlock FontSize="14" Foreground="#1F2933">
                        <Run Text="Total de Alunos: " FontWeight="Bold" Foreground="#236B73"/>
                        <Run Text="{Binding AlunosDaTurma.Count, Mode=OneWay, FallbackValue='0'}" />
                    </TextBlock>
                </StackPanel>
            </Border>

            <DataGrid Grid.Row="2" ItemsSource="{Binding AlunosDaTurma}" 
                      AutoGenerateColumns="False" 
                        Background="Transparent" 
                        BorderBrush="#CBD2D9"
                        BorderThickness="1" 
                        HeadersVisibility="Column" 
                        GridLinesVisibility="Horizontal"
                        HorizontalGridLinesBrush="#E2E8F0"
                        IsReadOnly="True"
                        RowHeight="38"
                        FontSize="14"
                        Foreground="#1F2933">


            <DataGrid.Resources>
                    <Style TargetType="DataGridCell">
                        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="BorderBrush" Value="Transparent"/>
                        <Setter Property="Padding" Value="10,0"/>
                    </Style>

                    <Style TargetType="DataGridRow">
                        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Style.Resources>
                            <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="Transparent" />
                            <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="#11181C" />
                            <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}" Color="Transparent" />
                            <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightTextBrushKey}" Color="#11181C" />
                        </Style.Resources>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#DDEEEB"/>
                                <Setter Property="Foreground" Value="#11181C"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                    <Style x:Key="AlinhamentoTextoStyle" TargetType="TextBlock">
                        <Setter Property="Padding" Value="12,0,0,0"/>
                        <Setter Property="VerticalAlignment" Value="Center"/>
                    </Style>
                </DataGrid.Resources>

                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#F0F4F8"/>
                        <Setter Property="Foreground" Value="#555555"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="Padding" Value="10,6"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="#CBD2D9"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Matrícula" Binding="{Binding Matricula}" Width="120" ElementStyle="{StaticResource AlinhamentoTextoStyle}"/>
                    <DataGridTextColumn Header="Nome" Binding="{Binding Nome}" Width="*" ElementStyle="{StaticResource AlinhamentoTextoStyle}"/>
                    <DataGridTextColumn Header="E-mail" Binding="{Binding Email}" Width="*" ElementStyle="{StaticResource AlinhamentoTextoStyle}"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Border>
</Window>